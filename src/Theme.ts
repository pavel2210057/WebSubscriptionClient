import { createTheme } from "@mui/material";

export const AppTheme = createTheme({
    palette: {
        // primary: {
        //     // main: "#dedede"
        // }
    },
    typography: {
        h1: {
            fontFamily: "Roboto, sans-serif",
            color: "#3d3d3d",
            letterSpacing: 0
        }
    }
})
